📋 Plan: Update Empty State for Product Grid in POS Dashboard
Context
In the POS dashboard, when there are no products to display (either because none exist or due to search/category filters), the ProductGrid component currently shows a generic empty state with an SVG icon and contextual messages. The requirement is to replace this with a specific "Tidak ada data yang ditampilkan" message and display the logo at @public/images/icon/no-task.svg.

Assumptions
- The image file @public/images/icon/no-task.svg exists in the public directory
- The empty state modification should only affect the ProductGrid component used in the POS dashboard
- No changes are needed to ProductCard.vue as it's only rendered when products exist
- The current empty state subtitle/actions are not required and can be removed

Scope
✅ In scope: Modify ProductGrid.vue empty state to show custom message and image
❌ Out of scope: Changes to ProductCard.vue, other components, or backend logic

Task Breakdown
Phase 1: Update ProductGrid Empty State [S]
 Task 1.1: Replace existing empty state SVG and messages with custom image and text
File: resources/js/components/pos/ProductGrid.vue
Details: 
  - Replace the SVG icon (lines 8-15) with an img tag pointing to /images/icon/no-task.svg
  - Replace the dynamic message (lines 16-18) with static text "Tidak ada data yang ditampilkan"
  - Remove the subtitle paragraph (lines 19-21) as it's not required
  - Maintain the same flex-col items-center justify-center py-16 text-center container classes
  - Keep the v-if condition (!loading && filteredProducts.length === 0) unchanged

Dependency Graph
Task 1.1

Risks & Mitigations
| Risk | Impact | Mitigation |
|------|--------|------------|
| Image path incorrect | Broken image display | Use absolute path from public directory (/images/icon/no-task.svg) and verify file exists |
| Styling inconsistencies | Visual mismatch with design | Maintain existing size classes (h-16 w-16) and spacing (mb-4) to match current SVG styling |
| Missing subtitle guidance | User confusion when no products | The primary message is clear; subtitle was only for search/add product hints which are less critical |

Handoff Notes
Handoff → Code Mode
Start with: Task 1.1 (ProductGrid.vue modification)
Key pattern: Direct DOM replacement in template section
Watch out for: Ensuring the image path is correct relative to the public directory